//
//  Project.swift
//  Manifests

import ProjectDescription
import ProjectDescriptionHelpers

let project = Project(
    name: "{{ name }}",
    targets: [
        .makeFeature(.{{ name|lowercase }}),
        .makeTestFeature(.{{ name|lowercase }})
    ]
)

